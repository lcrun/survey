{% extends "::base.html.twig" %}

{% block title %}{{ parent() }}{% endblock %}

{% block brand %}
    <a class="navbar-brand">{{ site_name }}</a>
{% endblock %}

{% block content %}
<br>
{# {% if not user.number %}#}
{# <form class="form-horizontal" role="form" action="{{ path('number_set') }}">#}
{#     <div class="form-group">#}
{#         <label for="number" class="col-sm-2 control-label">#}
{#             请先填写学号<span class="text-danger"> *</span>#}
{#         </label>#}
{#         <div class="col-sm-10">#}
{#             <input type="text" class="form-control" id="number" name="number" required="required">#}
{#         </div>#}
{#         <label for="number" class="col-sm-2 control-label">#}
{#             是否参加过上次培训#}
{#         </label>#}
{#         <div class="col-sm-10">#}
{#             <input type="checkbox" class="checkbox" id="participated" name="participated" value="1" />#}
{#         </div>#}
{#     </div>#}
{#     <div class="form-group">#}
{#         <div class="col-sm-offset-2 col-sm-10">#}
{#             <button type="submit" class="btn btn-default">提交</button>#}
{#         </div>#}
{#     </div>#}
{# </form>#}
{# {% else %}#}
<p>尊敬的{{ user.name }}老师：您好！</p>
<p>&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp首届新进教师研习营已经结束，为了进一步改进我们的工作，更好地服务一线教师，我们希望您能抽出一点时间，参加本次调查问卷。谢谢您的支持！</p>
  {#<dl class="dl-horizontal">
  <dt>姓名</dt>
  <dd>{{ user.name }}</dd>
  <dt>邮箱</dt>
  <dd>{{ user.code }}</dd>
 <dt>性别</dt>
 <dd>{{ user.gender }}</dd>
<dt>所在院系</dt>
  <dd>{{ user.department }}</dd>
  <dt>所在学科</dt>
  <dd>{{ user.subject }}</dd>
  <dt>任课情况</dt>
  <dd>{{ user.info }}</dd>
#}
  
  
  
</dl>

<ul>
    {% for studentExam in studentExams %}
    <li>
      {% if studentExam.isDone() %}
        <span>{{ studentExam.exam.title }}</span> [{{ studentExam.formatStatus }}]
      {% else %}
        <a href="{{ path('exam_view', {'id': studentExam.exam.id}) }}">{{ studentExam.exam.title }}</a> [{{ studentExam.formatStatus }}]
      {% endif %}

    </li>
    {% endfor %}
</ul>
{# {% endif %}#}

{% endblock %}
